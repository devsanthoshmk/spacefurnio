meta {
  name: Change Password
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/backend/api/v1/auth/change-password
  body: json
  auth: inherit
}

body:json {
  {
    "new-password": "newpassword"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
